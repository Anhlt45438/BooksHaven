export enum AccountStatus {
    Warning,
    Normal,
    Ban,
}

export enum TokenType {
    AccessToken,
    RefreshToken,
}
export enum RolesType {
    Admin = "admin",
    User = "user",
    Shop = "shop",
}